services:
  - type: web
    name: idreader-api-service
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn -w 2 -k uvicorn.workers.UvicornWorker main:app"
    envVars:
      - key: GOOGLE_API_KEY_2
        sync: false